# Semantica translation smoke test

Build a context graph with traceable decisions.

Install with `pip install semantica` and read the [documentation](https://docs.getsemantica.ai/).

```python
message = "This code must remain unchanged"
```

| Feature | Description |
| --- | --- |
| Provenance | Trace every decision |
